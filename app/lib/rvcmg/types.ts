/**
 * RVCMG Stage 0 scaffold — filled in for real in Stage 2. Kept as its own
 * file per the RVCMG implementation plan's deliverable list so Stage 0's
 * "project builds" acceptance check has something real to build against.
 */
export {};
